# Used by the local Hyperdrive binding only; never deployed as a Worker variable.
CLOUDFLARE_HYPERDRIVE_LOCAL_CONNECTION_STRING_HYPERDRIVE="postgres://postgres:supersecret@localhost:5432/postgres"
BETTER_AUTH_SECRET="generate-with-openssl-rand-base64-32"
BETTER_AUTH_URL="http://localhost:5173"
# Optional comma-separated additional public origins (for example, immutable preview URLs).
BETTER_AUTH_TRUSTED_ORIGINS=""
